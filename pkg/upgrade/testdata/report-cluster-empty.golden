UPGRADE CHECK
  from      cluster
  to        ./bundles-v0.17.0
  deployer  helm

READ FROM CLUSTER
  kubeconfig  /home/op/.kube/config
  context     staging-west
  matched     0 components
  helm        12 storage records, 1 unattributed, 2 unreadable, 4 uninstalled, 5 stamped but unmatched
  argo        0 applications, 0 unattributed, 0 unreadable, no stamp to check

  NOTHING INSTALLED WAS RECOGNIZED
  Nothing was compared, and every row below therefore reads "added". A cluster
  with none of these components installed reads this way, and so do the two
  likelier causes: this is not the cluster you meant, or these components were
  installed by a deployer other than the one this check was given, whose
  releases it does not look for.

COMPONENT       FROM  TO     VERDICT  NOTES
---------       ----  --     -------  -----
alpha-operator  -     1.2.3  -        new component, nothing to do
zeta-operator   -     0.4.0  -        new component, nothing to do

AT RISK (advisory)
  Objects of the kinds the crossed transition records name that carry neither
  Helm nor Argo CD ownership. AICR does not manage them and cannot restore
  them; removing a CRD takes its objects with it. Advisory only: this never
  changes the exit code.

  No crossed transition record names a resource kind, so nothing was examined.

2 component changes, 0 need attention
