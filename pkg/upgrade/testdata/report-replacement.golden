UPGRADE CHECK
  deployer  helm

COMPONENT  FROM     TO     VERDICT  NOTES
---------  ----     --     -------  -----
newgate    oldgate  1.3.1  manual   replaces oldgate, 2 steps

newgate replaces oldgate  (manual)
  oldgate is superseded by newgate for inference routing.

  recorded manual: newgate replaces oldgate, which is migration work rather
  than a version bump

  STEPS (deployer: helm)
    1. port-route-resources
       Re-author the oldgate route resources as newgate equivalents.
       Why: nothing migrates them automatically; field names and defaults
       differ.
    2. retire-oldgate
       Uninstall the oldgate release once newgate is serving traffic.
       Why: AICR leaves it installed and will not remove it.

AT RISK (advisory)
  Objects of the kinds the crossed transition records name that carry neither
  Helm nor Argo CD ownership. AICR does not manage them and cannot restore
  them; removing a CRD takes its objects with it. Advisory only: this never
  changes the exit code.

  not scanned: no cluster access requested

1 component change, 1 needs attention
