UPGRADE CHECK
  from      ./bundles-v0.16.0
  to        ./bundles-v0.17.0
  deployer  argocd

COMPONENT         FROM    TO         VERDICT      NOTES
---------         ----    --         -------      -----
alpha-operator    1.2.0   1.2.3      safe         3 patches, verified, safe across 1 minor
beta-operator     0.17.2  0.18.1     manual       1 minor, 1 step, manual across 3 minors
delta-operator    0.18.0  0.19.0     unknown      1 minor, no record, breaking boundary
epsilon-operator  main    release-1  unversioned  versions are not comparable
eta-operator      2.4.1   -          -            stays installed; AICR does not uninstall it
gamma-operator    1.0.0   3.0.0      blocked      2 majors, stops at >=2.0.0 <3.0.0
kappa-operator    3.2.0   4.0.0      blocked      1 major, stops at >=4.0.0 <=4.0.0, 2 steps
lambda-operator   1.1.0   1.2.0      unknown      1 minor, record exists, no boundary here
mu-operator       1.17.0  1.25.0     blocked      8 minors, stops at >=1.18.0 <1.19.0
theta-operator    0.13.0  0.11.0     unknown      downgrade, unassessable
zeta-operator     -       0.4.0      -            new component, nothing to do

beta-operator 0.17.2 -> 0.18.1  (manual)
  The legacy API group is renamed. A mirror controller copies existing
  objects.

  recorded manual: the record covering 0.18.0 describes the move from 0.17.2
  to 0.18.1. Follow the steps below, then apply

  PRECONDITION
    No object is mid-rollout and no node is mid-package.

  STEPS (deployer: argocd)
    1. rename-crs
       In a single commit, remove the legacy manifests and add their
       replacements.
       Why: one commit lets the controller prune and adopt in a single sync.

gamma-operator 1.0.0 -> 3.0.0  (blocked)
  crosses 2 recorded boundaries (2.0.0, 3.0.0); no single record describes the
  whole jump. Upgrade to 2.0.0 first, then re-run this check

kappa-operator 3.2.0 -> 4.0.0  (blocked)
  4.0.0 drops in-place conversion; the store has to be exported and reloaded.

  recorded blocked: the record covering 4.0.0 describes the move from 3.2.0 to
  4.0.0 and blocks it in one step. Follow the steps below to get there safely

  PRECONDITION
    No writer is connected to the store.

  STEPS (deployer: argocd)
    1. export-store
       Export the store with the 3.x tooling before touching the release.
       Why: 4.0.0 cannot read the 3.x on-disk format, and the converter was
       removed.
    2. land-on-3-9-first
       Upgrade to 3.9.x, reload the export there, then move to 4.0.0.

mu-operator 1.17.0 -> 1.25.0  (blocked)
  transition records for this component assess only as far as the last version
  below 1.19.0, and 1.25.0 lands past that, so nothing assesses this move or
  anything above the last version below 1.19.0. Upgrade no further than the
  last version below 1.19.0 and re-run this check, or widen a record's `to`
  range to cover 1.25.0

11 component changes, 8 need attention
