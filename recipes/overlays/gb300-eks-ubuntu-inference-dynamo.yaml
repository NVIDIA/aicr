# Copyright (c) 2026, NVIDIA CORPORATION & AFFILIATES.  All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.


kind: RecipeMetadata
apiVersion: aicr.run/v1alpha2
metadata:
  name: gb300-eks-ubuntu-inference-dynamo

spec:
  # Inherits from gb300-eks-ubuntu-inference (GB300 + Ubuntu inference settings)
  # Adds Dynamo inference platform components.
  base: gb300-eks-ubuntu-inference

  criteria:
    service: eks
    accelerator: gb300
    os: ubuntu
    intent: inference
    platform: dynamo

  # DRA requires Kubernetes 1.34+ (GA)
  constraints:
    - name: K8s.server.version
      value: ">= 1.34"

  componentRefs:
    - name: grove
      type: Helm
      source: oci://ghcr.io/ai-dynamo/grove
      valuesFile: components/grove/values.yaml

    - name: dynamo-platform
      type: Helm
      source: https://helm.ngc.nvidia.com/nvidia/ai-dynamo
      valuesFile: components/dynamo-platform/values.yaml
      dependencyRefs:
        - grove
        - cert-manager
        - kube-prometheus-stack
        - gpu-operator
        - kai-scheduler

  validation:
    performance:
      checks:
        - inference-perf
      # Conservative smoke gate mirrored from the GB200 EKS Dynamo overlay
      # until GB300-specific inference baselines are available. GB300 NVL72
      # exceeds GB200 per-GPU, so the GB200-derived floor should not false-fail
      # a healthy run — it must be re-baselined on real GB300 hardware before
      # it is treated as a regression gate. Model + concurrency are pinned so
      # the gate stays valid independent of the compiled validator defaults.
      #
      # NOTE: this throughput floor is a fixed absolute full-node value (here a
      # 4-GPU GB300 node) and is not normalized for GPU count, so a smaller SKU
      # of this accelerator can false-fail a healthy run. A normalized per-GPU
      # floor is tracked in https://github.com/NVIDIA/aicr/issues/1254.
      constraints:
        - name: inference-model
          value: Qwen/Qwen3-8B
        - name: inference-concurrency-per-gpu
          value: "256"
        - name: inference-routing-mode
          value: dynamo-router
        - name: inference-throughput
          value: ">= 50000"
        - name: inference-ttft-p99
          value: "<= 2000"
    conformance:
      checks:
        - platform-health
        - gpu-operator-health
        - dra-support
        - accelerator-metrics
        - ai-service-metrics
        - inference-gateway
        - robust-controller
        - secure-accelerator-access
        - pod-autoscaling
        - cluster-autoscaling
